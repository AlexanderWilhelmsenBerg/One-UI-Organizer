package io.github.alexanderwilhelmsenberg.oneuiorganizer.model

/** App-owned destination for opening Organizer focused on one durable category identity. */
data class CategoryShortcutDestination(val categoryId: CategoryId)
